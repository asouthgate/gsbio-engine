name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install Engine dependencies
        run: npm ci

      - name: Build Engine Core
        run: npm run build

      - name: Test Engine Core
        run: npm test

      - name: Install Example dependencies
        run: npm ci --prefix examples/demo-web

      - name: Build Example App
        run: npm run build --prefix examples/demo-web

      - name: Integration test (requires backend — disabled by default)
        if: false
        run: node test_pmtiles_engine.mjs http://localhost:8000 test.pmtiles

      - name: Upload demo-web build artifact
        uses: actions/upload-artifact@v4
        with:
          name: demo-web
          path: examples/demo-web/dist